import type { FC } from 'react';
import { Typography } from 'antd';

const SavedTab: FC = () => {
  return (
    <div style={{ padding: '8px 0' }}>
      <Typography.Text type="secondary">
        Saved strategies — content placeholder.
      </Typography.Text>
    </div>
  );
};

export default SavedTab;
