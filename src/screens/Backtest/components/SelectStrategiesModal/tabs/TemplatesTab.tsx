import type { FC } from 'react';
import { Typography } from 'antd';

const TemplatesTab: FC = () => {
  return (
    <div style={{ padding: '8px 0' }}>
      <Typography.Text type="secondary">
        Strategy templates — content placeholder.
      </Typography.Text>
    </div>
  );
};

export default TemplatesTab;
